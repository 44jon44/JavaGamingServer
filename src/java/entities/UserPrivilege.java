/*
 * Enum con el privilegio del usuario. Puede ser Admin, Client o Employee
 */
package entities;

/**
 *
 * @author Alex Hurtado
 * 
 * privilegios que pueden tener un usuario
 */
public enum UserPrivilege {
    ADMIN,
    CLIENT,
    EMPLOYEE
}
